// export default means when you want to export only one thing from a file/module
// then use export default
const sayMyName = (name)=>{
    console.log(`Welcome ${name}!!!`);
}

export default sayMyName;